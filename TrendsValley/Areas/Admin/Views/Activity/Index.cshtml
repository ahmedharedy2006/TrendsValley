@model IEnumerable<TrendsValley.Models.Models.AdminActivity>


<link href="~/css/InProduct.css" rel="stylesheet" />

<div class="main-content">

    <main>

        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4 p-1">
            <div>
                <p class="text-3xl font-semibold dark:text-black">
                    <span class="fa-solid fa-list"></span>  Activities
                </p>
            </div>
            <div>
            </div>
        </div>

        <br />
        <hr />
        <br />
        <table id="filter-table" border="1">
            <thead class="dark:bg-blue-600 text-white">
                <tr>
                    <th>
                        <span class="flex items-center">
                            User Name
                            <svg class="w-4 h-4 ms-1" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24">
                                <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m8 15 4 4 4-4m0-6-4-4-4 4" />
                            </svg>
                        </span>
                    </th>
                    <th>
                        <span class="flex items-center">
                            Actions

                        </span>
                    </th>
                    <th>
                        <span class="flex items-center">
                            Date

                        </span>
                    </th>

                </tr>
            </thead>
            <tbody>
                @{
                    int i = 1;
                }
                @foreach (var Activity in Model)
                {
                    <tr>

                        <td class="font-medium text-gray-900 whitespace-nowrap dark:text-black">@Activity.User.Fname @Activity.User.Lname</td>
                        <td class="font-medium text-gray-900 whitespace-nowrap dark:text-black">
                            @Activity.Description
                        </td>
                        <td class="font-medium text-gray-900 whitespace-nowrap dark:text-black">
                            @Activity.ActivityDate
                        </td>
                    </tr>

                    i++;
                }
            </tbody>
        </table>
    </main>

</div>

@section Scripts {
    <script src="~/js/categories.js"></script>
}